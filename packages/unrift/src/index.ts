export {
  beforeEach,
  afterEach,
  beforeAll,
  afterAll,
  defineConfig,
  describe,
  it,
  expect,
  extendMatchers,
} from "@unrift/core";

export type {
  Matchers,
  ExpectInterface,
  MatcherContext,
  MatcherFn,
  MatcherMap,
} from "@unrift/core";
