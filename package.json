{
  "name": "@unrift/monorepo",
  "version": "0.0.0",
  "description": "Monorepo for the Unrift TypeScript test framework",
  "keywords": ["testing", "test-runner", "typescript", "esm"],
  "author": "elijah kotyluk <elijah@elijahkotyluk.com>",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "clean": "pnpm run -r clean",
    "build": "pnpm run --filter '!@unrift/docs' -r build",
    "build:all": "pnpm run -r build",
    "test": "pnpm run --filter '!@unrift/docs' -r test",
    "test:all": "pnpm run -r test",
    "typecheck": "pnpm run --filter '!@unrift/docs' -r typecheck",
    "typecheck:all": "pnpm run -r typecheck",
    "docs:dev": "pnpm --filter @unrift/docs dev",
    "docs:build": "pnpm --filter @unrift/docs build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "prepare": "husky",
    "lerna:version": "npx lerna version --no-private",
    "lerna:publish": "npx lerna publish"
  },
  "devDependencies": {
    "@eslint/js": "~9.38.0",
    "@types/node": "^24.10.1",
    "devoid": "^0.2.0",
    "eslint": "~9.38.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "~5.5.0",
    "globals": "~16.4.0",
    "husky": "^9.1.7",
    "lerna": "^9.0.1",
    "prettier": "~3.6.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "~8.46.0"
  }
}
